"""Agent harnesses. src.agents.codex_mcp_loop is the AGENT_HARNESS=codex_mcp implementation
-- Codex driving the data-operations lifecycle entirely through MCP tool calls (src/mcp_servers/)
instead of src.data_ops's direct Python calls (AGENT_HARNESS=current, the default, untouched).
"""
