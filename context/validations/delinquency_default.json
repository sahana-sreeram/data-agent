{
  "tolerance": {
    "currency": 0.01,
    "count": 0,
    "rate": 0.0001
  },
  "notes": [
    "Reconciliation-only. Executed by src/validate_delinquency_default.py, which independently recomputes the overall row and every risk_segment row in pandas (never importing src/etl_spark_delinquency_default.py's Spark logic)."
  ],
  "rules": [
    {"id": "delinquency_default_breakdown_rows_match", "type": "reconciliation", "tolerance_type": "count", "description": "Every breakdown_value row (the overall 'ALL' row plus one per risk_segment) in curated/delinquency_default.parquet must match an independent pandas recomputation of loan_count, total_funded_principal, delinquent_loan_count, delinquency_rate, default_count, default_rate, total_balance_at_default, total_recovery_amount, recovery_rate, and loss_rate."}
  ]
}
